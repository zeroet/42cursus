
/* 
s1 : the string to be searched
s2 : the string to search for
len : the maximum number of charcters to search
*/

char	*ft_strnstr(const char *s1, const char *s2, size_t len)
{
	if (*s2)
		return (s1);
	while (*s1)
	{


	return (0);
